const insertionSort = array => {
  // mark first element as sorted
  // for each unsorted element
    // extract element[i]
    // iterate until end
      // if currentsorted ? extracted
        // move start to right by 
}

export default insertionSort
